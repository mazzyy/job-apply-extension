{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser-pane",
  "description": "The integrated browser pane: emit/listen events + a proxied backend_call so injected engines can reach the local backend on any job site (CSP-proof). No other app commands.",
  "webviews": ["browser"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*", "http://localhost:*/*", "https://*/*"]
  },
  "permissions": ["core:event:allow-emit", "core:event:allow-listen", "allow-browser-backend"]
}
